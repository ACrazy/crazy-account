const url = "https://www.crazyfisher.cn";
const getHeader = {
  'content-type': 'application/json' // 默认值
}
const postHeader = {
  'content-type': 'application/x-www-form-urlencoded' // 默认值
}

module.exports = {
  url: url,
  getHeader: getHeader,
  postHeader: postHeader
}